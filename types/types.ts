export type UserType = {
    id: string
    name: string
    email: string
    username: string
    bio: string
    image: string
}

export type PostType = {
    id: string
    content: string
    //user: UserType
}